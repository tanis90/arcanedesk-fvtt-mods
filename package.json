{
  "name": "arcanedesk-fvtt-mods",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "test": "node modules/arcane-common-display-vision/scripts/validate.mjs && node --test tests/*.test.mjs",
    "build": "node tools/build.mjs",
    "verify": "npm test && npm run build"
  },
  "devDependencies": {
    "fflate": "0.8.3"
  }
}
